---
title: "Stamp River Chinook Escapement"
output: flexdashboard::flex_dashboard
runtime: shiny
---

```{r setup, include=FALSE}
library(flexdashboard)
library(tidyverse)
library(geomtextpath)

source("build_cn_timing_plot.R")

# stamp_cn, curr_year, esc_target loaded/defined here as usual
```

Sidebar {.sidebar}
=====================================

```{r}
sliderInput(
  "hist_years",
  "Historic timing window (years):",
  min = 5, max = 25, value = 20, step = 1
)
```

Column
=====================================

### Escapement Timing

```{r}
renderPlot({
  build_cn_timing_plot(
    data       = stamp_cn,
    curr_year  = curr_year,
    hist_years = input$hist_years,
    esc_target = esc_target
  )
})
```
